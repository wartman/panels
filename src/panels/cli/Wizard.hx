package panels.cli;

import tink.cli.Prompt;

// @todo: This will offer some handy promts to guide the
// user through the generation process. One day.
class Wizard {
  @:defaultCommand
  public function run(prompt:Prompt) {}
}
